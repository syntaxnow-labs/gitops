apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
spec:
  template:
    spec:
      containers:
        - name: sample-app
          image: "ghcr.io/syntaxnow-labs/sample-app:prod-latest"
