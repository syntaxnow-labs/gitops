apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps-qa

resources:
  - ../../base
  - secrets.yaml
  - namespace.yaml
  - ingress.yaml

configMapGenerator:
  - name: mock-api-service-config
    behavior: create
    envs:
      - values-qa.env

patches:
  - path: patch-image.yaml
