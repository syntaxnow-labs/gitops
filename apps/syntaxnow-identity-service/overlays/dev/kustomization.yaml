apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps-dev

resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml
  - secrets.yaml

configMapGenerator:
  - name: syntaxnow-identity-service-config
    behavior: create
    files:
      - values-dev.yaml

patches:
  - path: patch-image.yaml
