apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vm-0656-root
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/syntaxnow-labs/gitops.git
    targetRevision: main
    path: clusters/vm-0656

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
